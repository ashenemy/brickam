# @brickam/payments

Платежи BuildHub (Stage 5, Foundations §11/§15): один платёж на заказ с разбивкой
по вендорам (`splits`). Эскроу нет. Провайдер — за абстракцией `PaymentProvider`
(по умолчанию `MockPaymentProvider`).
