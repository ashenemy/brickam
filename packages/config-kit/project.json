{
    "name": "config-kit",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "packages/config-kit/src",
    "projectType": "library",
    "targets": {
        "build": {
            "executor": "@nx/js:tsc",
            "outputs": ["{options.outputPath}"],
            "options": {
                "outputPath": "dist/packages/config-kit",
                "main": "packages/config-kit/src/index.ts",
                "tsConfig": "packages/config-kit/tsconfig.lib.json",
                "assets": ["packages/config-kit/*.md"]
            }
        },
        "test": {
            "executor": "nx:run-commands",
            "outputs": ["{projectRoot}/coverage"],
            "options": {
                "cwd": "packages/config-kit",
                "command": "npx vitest run --coverage"
            }
        }
    },
    "tags": ["type:kit", "scope:shared"]
}
