{
    "name": "db-kit",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "packages/db-kit/src",
    "projectType": "library",
    "targets": {
        "build": {
            "executor": "@nx/js:tsc",
            "outputs": ["{options.outputPath}"],
            "options": {
                "outputPath": "dist/packages/db-kit",
                "main": "packages/db-kit/src/index.ts",
                "tsConfig": "packages/db-kit/tsconfig.lib.json",
                "assets": ["packages/db-kit/*.md"]
            }
        },
        "test": {
            "executor": "nx:run-commands",
            "outputs": ["{projectRoot}/coverage"],
            "options": {
                "cwd": "packages/db-kit",
                "command": "npx vitest run --coverage"
            }
        }
    },
    "tags": ["type:kit", "scope:server"]
}
