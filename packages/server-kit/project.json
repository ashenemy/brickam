{
    "name": "server-kit",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "packages/server-kit/src",
    "projectType": "library",
    "targets": {
        "build": {
            "executor": "@nx/js:tsc",
            "outputs": ["{options.outputPath}"],
            "options": {
                "outputPath": "dist/packages/server-kit",
                "main": "packages/server-kit/src/index.ts",
                "tsConfig": "packages/server-kit/tsconfig.lib.json",
                "assets": ["packages/server-kit/*.md"]
            }
        },
        "test": {
            "executor": "nx:run-commands",
            "outputs": ["{projectRoot}/coverage"],
            "options": {
                "cwd": "packages/server-kit",
                "command": "npx vitest run --coverage"
            }
        }
    },
    "tags": ["type:kit", "scope:server"]
}
