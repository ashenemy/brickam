# BuildHub — готовые промпты по стейджам (v8)

Как пользоваться: держи в Claude Code загруженными `buildhub_foundations_v8.md` (закон) и `buildhub_stages_v8.md`. Затем по очереди копируй блок нужного стейджа ниже и вставляй. Один стейдж = одна итерация. Стейдж закрыт только при всех ✓.

**Правила во всех стейджах:** `type` вместо `interface`; максимум ООП; типы в `src/@types/index.ts`; никакого хардкода (TOML/env или админ-настройки в БД); переиспользуемые DTO (mapped-types); generic CRUD/пагинация; глобальные interceptors + `AllExceptionsFilter` + кастомные ошибки; Swagger; Nx tags+границы (feature ↛ feature); Biome (4 пробела, одинарные кавычки)+ESLint без конфликтов; Prettier нет; **коммиты по одному скоупу**; **тесты в каждом стейдже, покрытие ≥80%**. После стейджа: зелёная сборка, чистый линт, целые границы, открытый `/api/docs`, отчёт по чек-листу.

===STAGE-SPLIT===
# ▶ STAGE 0 — Каркас и DevOps-фундамент
Продолжаем сборку BuildHub. Foundations = `buildhub_foundations_v8.md` (закон). Выполни ТОЛЬКО Stage 0.

**Цель:** рабочий монорепо, на котором всё последующее само соблюдает стандарты.

**Сделай:**
- Инициализируй Nx-воркспейс (integrated), layout `packages/` (см. `nx.json` из стартера). Создай apps `main-web`, `vendor-web`, `admin-web` (Angular standalone), `server` (NestJS).
- Перенеси стартовые конфиги в корень: `biome.json`, `config/*.toml`, `.env.example`, `commitlint.config.cjs`, `lefthook.yml`, `tools/hooks/single-scope-guard.mjs`, `tsconfig.base` (strict-сниппет), eslint flat-config с `@nx/enforce-module-boundaries` (см. сниппет). Проставь Nx tags каждому проекту (`type:*`, `scope:*`).
- `packages/config-kit`: загрузка TOML (`smol-toml`) + deep-merge по `NODE_ENV` + overlay env; валидация конфига на старте (fail-fast); типизированный `AppConfigService`. Для фронтов — рантайм `assets/config.json` + `APP_INITIALIZER`.
- `packages/core-kit`: абстрактный `AppException{code,httpStatus,messageKey,details?}`, enum `ErrorCode` (каталог из Foundations §8), подклассы ошибок, `Result`-тип, утилиты.
- `packages/server-kit`: глобальные interceptors в порядке RequestContext(traceId)→Logging→Timeout→ClassSerializer→ResponseTransform; `AllExceptionsFilter` (конверт ошибки); глобальный `ValidationPipe` (whitelist+forbidNonWhitelisted+transform); `PaginatedDto<T>`, `PaginationQueryDto`, `ApiResponse<T>`; composite-декораторы `@Auth(...perms)`, `@ApiPaginatedOk(model)`, `@CurrentUser()`, `@CurrentVendor()`, `@Serialize(Dto)`; абстрактные `BaseCrudService<T,C,U>`, `BaseCrudController<T,C,U>`.
- `packages/db-kit`: подключение Mongo (URI из env), `BaseSchema`, generic `BaseRepository<T>` (find/findPaginated/create/update/delete).
- Swagger на `/api/docs` (+`/api/docs-json`); настрой генерацию `packages/api-kit` из OpenAPI (orval/openapi-generator).
- `packages/i18n-kit`: каркас словарей hy/ru/en + ключи ошибок.
- `infra/`: `Dockerfile.server`, `Dockerfile.main-web|vendor-web|admin-web` (build→nginx), `docker-compose.yml` (Mongo+Redis+server+3 web). `/api/health`.

**Тесты:** юнит — валидация конфига (нет обязательного → падение), `BaseRepository` (CRUD+пагинация), `ResponseTransformInterceptor`, `AllExceptionsFilter` (маппинг AppException→конверт); смоук-e2e `/api/health`.

**✓ Чек-лист:**
- [ ] монорепо собирается; `biome check` и `eslint` зелёные, без взаимных конфликтов
- [ ] границы Nx работают (нарушение импорта падает)
- [ ] коммит из 2 проектов отклоняется guard-хуком; commitlint требует scope
- [ ] конфиг грузится из TOML+env, валидируется на старте; фронты читают рантайм-конфиг
- [ ] унифицированный конверт успеха/ошибки работает; ошибки локализуемы
- [ ] Swagger открыт; `api-kit` генерится из OpenAPI
- [ ] `docker-compose up` поднимает стек; `/api/health` отвечает
- [ ] тесты зелёные, покрытие ≥80%

Отчёт по чек-листу. Для Stage 1 мне понадобится положить React-UI-kit в `/_input/react-ui-kit/` и дать Figma — напомни об этом.

===STAGE-SPLIT===
# ▶ STAGE 1 — Дизайн-система, перенос React-UI-кита, каркасы
Выполни ТОЛЬКО Stage 1. Foundations §5, §5а — закон.

**Цель:** единые токены и `ui-kit` на Angular+Material+Tailwind, перенесённый с React (без багов, адаптивный), плюс оболочки трёх приложений.

**Сделай:**
- `packages/design-tokens`: `tokens.json` (цвета RGB-каналами + spacing/radius/typography; часть значений вытащи из React-кита) → генерация `tokens.css` (`:root{--color-*}`). Tailwind-конфиг ссылается на `var(--…)`; Angular Material 3 — проброс в `--mat-sys-*`; экспорт TS-объекта для mobile.
- **Перенос React-UI-кита** из `/_input/react-ui-kit/` в `packages/ui-kit`: каждый React-компонент → Angular **standalone**. Маппинг: `useState/useReducer`→signals; props→`input()`; колбэки/события→`output()`; `children`→`ng-content`; условный рендер→`@if/@for`; контекст→Angular DI/сервис. Где есть аналог — Angular Material; стили — Tailwind через токены (никаких хардкод-цветов).
- **Исправь баги исходника** (типизация, состояния) и **доведи респонсивность** (mobile-first, брейкпоинты, без overflow). Добавь a11y (роли, фокус, контраст) и 3 языка.
- Шеллы навигации `main-web`/`vendor-web`/`admin-web`; переключатель языков hy/ru/en; gate входа по роли.
- Сверка с Figma; при расхождении приоритет Figma, расхождения зафиксируй в `ui-kit/MIGRATION_NOTES.md`.

**Тесты:** компонент-тест на каждый перенесённый компонент (рендер, inputs/outputs, состояния); тест «смена токена меняет и Tailwind-, и Material-значение»; тесты адаптивности (брейкпоинты/overflow); тест переключателя языка.

**✓ Чек-лист:**
- [ ] React-кит полностью перенесён на Angular+Material+Tailwind, баги устранены
- [ ] компоненты адаптивны, доступны, без хардкод-цветов (всё через токены)
- [ ] правка одного токена отражается во всех приложениях
- [ ] 3 приложения запускаются, язык переключается, вход гейтится по роли
- [ ] расхождения с Figma задокументированы
- [ ] тесты/покрытие ок

Отчёт по чек-листу.

===STAGE-SPLIT===
# ▶ STAGE 2 — Auth и пользователи
Выполни ТОЛЬКО Stage 2. Foundations §13, §14.

**Цель:** регистрация/вход по телефону+паролю+OTP, роли.

**Сделай:** `packages/feature/auth` + `feature/users`.
- Эндпоинты: `POST /auth/register` (+374, пароль, роль)→OTP; `POST /auth/verify-otp`; `POST /auth/login`; `POST /auth/refresh`; `POST /auth/forgot`; `POST /auth/reset`.
- Сервисы (классы): `OtpService` (длина/TTL/попытки/кулдаун из конфига, хеш кода, rate-limit), `TokenService` (JWT access+refresh), `AuthService`, `UsersService` (на `BaseCrudService`).
- Вендор после регистрации — `status: pending`. Вход с нового устройства — доп. OTP (флаг).
- Guards: `JwtAuthGuard`, `PermissionsGuard` (права из Foundations §14, без `tenders.respond`).
- Тексты OTP-SMS — заглушка-провайдер (mock пишет код в лог); реальный текст подключится в Stage 3 через templates.
- `passwordHash` помечен `@Exclude()` (ClassSerializer не отдаёт).

**Тесты:** юнит OTP (генерация/TTL/попытки/кулдаун/хеш/rate-limit); e2e register→verify→login→refresh; негативы (неверный/просроченный OTP, лимит); проверка ролевых guard'ов и `@Auth(...perms)`.

**✓ Чек-лист:**
- [ ] полный цикл auth на моке SMS работает
- [ ] access/refresh корректны; refresh ротация безопасна
- [ ] OTP-лимиты и хеширование соблюдаются; пароль не утекает в ответах
- [ ] роли/права гейтят доступ
- [ ] тесты/покрытие ок

===STAGE-SPLIT===
# ▶ STAGE 3 — Шаблоны и уведомления
Выполни ТОЛЬКО Stage 3. Foundations §14, §10.

**Цель:** тексты писем/SMS не в коде с самого начала.

**Сделай:** `feature/templates` + `feature/notifications`.
- `templates` (коллекция из §15): email/sms, мультиязычные `content{hy,ru,en}` (+`subject` для email), белый список `variables`, версия. CRUD-бэкенд (UI-редактор будет в Stage 17).
- Шаблонизатор (Handlebars/Liquid): рендер с подстановкой и валидацией переменных (неизвестная/пропущенная → ошибка `ValidationError`).
- `notifications`: интерфейсы каналов (`SmsChannel`, `EmailChannel`) за провайдерами; тело берётся ТОЛЬКО из templates по `key`. OTP-SMS из Stage 2 переключи на шаблон.
- Дефолтные шаблоны (OTP, подтверждение заказа, статус заказа, инвойс, сброс пароля) — добавятся и в сид (Stage 14), но базовые создай здесь.

**Тесты:** юнит рендера+валидации переменных; тест «OTP/уведомления берут текст из templates, не из строк»; рендер на 3 языках.

**✓ Чек-лист:**
- [ ] ни одного хардкод-текста уведомлений
- [ ] шаблоны рендерятся на hy/ru/en, переменные валидируются
- [ ] каналы за провайдер-интерфейсами (mock в тестах)
- [ ] тесты/покрытие ок

===STAGE-SPLIT===
# ▶ STAGE 4 — Каталог + SEO
Выполни ТОЛЬКО Stage 4. Foundations §12, §13, §15.

**Цель:** товары на продажу, обложка фото/видео, поиск/фильтры, SEO.

**Сделай:** `feature/catalog`.
- Сущности `categories`, `products` (из §15; БЕЗ rental — только продажа). `cover.mediaType` image|video + `gallery`, `discount?{type,value,окно}`, `attributes`, `embedding` (заполняется в Stage 13).
- CRUD товаров/категорий на `BaseCrudService/Controller`; mapped-types DTO.
- Поиск/фильтры (категория, цена, поставщик, рейтинг, наличие, регион), сортировка, **серверная пагинация** (`PaginationQueryDto`/`PaginatedDto`). Текстовый индекс по title/description.
- `main-web`: листинг + карточка товара. Видео-обложка: автоплей muted loop во вьюпорте, `thumbnailUrl`-фолбэк; на mobile — нативный плеер (учтётся в Stage 19).
- Валидация загружаемого медиа по `platform_settings.media` (форматы/размер/длительность/размеры).
- **SEO:** Angular SSR-сервис (`@angular/ssr`); на edge/nginx — детект бот-UA (список из `platform_settings.seo.botUserAgents`)→SSR, люди→CSR; пререндер каталожных/товарных маршрутов; `meta/OG`, `sitemap.xml`, `robots.txt`.

**Тесты:** юнит фильтров/пагинации/цены-со-скидкой; e2e поиск/фильтр/карточка; тест видео-vs-фото обложки+фолбэк; тест маршрутизации бот→SSR / человек→CSR; тест валидации медиа по лимитам.

**✓ Чек-лист:**
- [ ] каталог ищется/фильтруется/пагинируется (серверно)
- [ ] обложка-видео работает с фолбэком; лимиты медиа применяются
- [ ] бот получает SSR-HTML, человек — CSR; sitemap/robots отдаются
- [ ] тесты/покрытие ок

===STAGE-SPLIT===
# ▶ STAGE 5 — Корзина, заказ, платёж с разбивкой, комиссия
Выполни ТОЛЬКО Stage 5. Foundations §11 (бизнес-правила!), §15.

**Цель:** оформление и ОДИН платёж, разбитый по вендорам; комиссия с продавца после скидки в AMD. Эскроу НЕТ.

**Сделай:** `feature/orders` + `feature/payments`.
- `carts` (группировка позиций по вендору). Оформление: адрес, хук под скидку лояльности (применится в Stage 12).
- При оформлении строй `order` + по одному `vendor_order` на вендора. Расчёт (целые AMD):
  - `vendorSubtotalAfterDiscount` = сумма позиций вендора после товарных скидок;
  - `commissionAmount = round(vendorSubtotalAfterDiscount * commissionPercent/100)` (процент из конфига);
  - `payoutAmount = vendorSubtotalAfterDiscount − commissionAmount`.
- Один `payment` (заглушка провайдера) на сумму заказа; `payment.splits[]` = по вендорам `{amount, commissionAmount, payoutAmount}`.
- Статусы заказа (created→paid→processing→completed→cancelled) и доставки в `vendor_orders.deliveryStatus` + `deliveryEvents[]`. Номера заказов.
- Списание `stock`; проверка наличия.

**Тесты:** юнит расчёта (subtotal→комиссия после скидки→payout→splits, округление AMD), мультивендорный кейс; e2e оформление→оплата(заглушка)→`vendor_orders`+`splits`; негативы (пустая корзина, нет остатка).

**✓ Чек-лист:**
- [ ] один платёж корректно разбивается по вендорам
- [ ] комиссия = на сумму после скидки, целые AMD; payout верный
- [ ] статусы заказа/доставки меняются; stock списывается
- [ ] эскроу отсутствует
- [ ] тесты/покрытие ок

===STAGE-SPLIT===
# ▶ STAGE 6 — Отложенные (вишлист)
Выполни ТОЛЬКО Stage 6.

**Сделай:** `feature/wishlist`. Коллекция `wishlists` (userId уник, items[{productId,addedAt}]). Эндпоинты add/remove/list (идемпотентно). UI `main-web`: сердечко на карточках/в карточке, страница «Отложенные», «в корзину», бейдж-счётчик.

**Тесты:** юнит add/remove/идемпотентность; e2e добавление→страница→в корзину.

**✓ Чек-лист:**
- [ ] товар откладывается/убирается без дублей, переносится в корзину
- [ ] счётчик верен; тесты/покрытие ок

===STAGE-SPLIT===
# ▶ STAGE 7 — Отзывы и рейтинги
Выполни ТОЛЬКО Stage 7.

**Сделай:** `feature/reviews`. Отзыв (1–5+текст) только по **завершённому** заказу; привязка к `orderId/vendorOrderId/vendorId/productId?`. Пересчёт `ratingAvg/ratingCount` вендора и товара. Отображение в карточке/профиле вендора. Флаг модерации `status(published|hidden)`. Защита от повторного отзыва на тот же `vendor_order`.

**Тесты:** юнит пересчёта рейтинга; e2e «только после completed»; повторный отзыв отклоняется.

**✓ Чек-лист:**
- [ ] отзыв возможен лишь по завершённому заказу, без повторов
- [ ] рейтинги пересчитываются корректно; тесты/покрытие ок

===STAGE-SPLIT===
# ▶ STAGE 8 — Чат
Выполни ТОЛЬКО Stage 8.

**Сделай:** `feature/chat`. Socket.IO Gateway; коллекции `chats`/`messages`. Список диалогов, непрочитанные (`unread{buyer,vendor}`), статусы прочтения (`readBy`), текст+файлы. Авторизация: доступ к каналу только участникам. UI покупателя в `main-web` (вендор-UI — в Stage 15). Опц. Redis-adapter для масштабирования.

**Тесты:** юнит unread/прочтения; e2e обмен сообщениями двух ролей; запрет доступа постороннего к чужому чату.

**✓ Чек-лист:**
- [ ] реалтайм-чат работает; непрочитанные/прочтения корректны
- [ ] доступ только участникам; тесты/покрытие ок

===STAGE-SPLIT===
# ▶ STAGE 9 — Кастомные инвойсы
Выполни ТОЛЬКО Stage 9. Foundations §15.

**Сделай:** `feature/invoices`. Продавец с правом `invoices.create` формирует инвойс покупателю прямо в диалоге (`lineItems`, `discount?`, `validUntil`)→сообщение типа `invoice` в чат. Оплата инвойса → заказ (через Stage 5, разбивка/комиссия применяются). Генерация PDF (`pdfUrl`). Статусы draft|sent|paid|expired|cancelled; авто-expire по `validUntil`. Вендор-UI создания — в `vendor-web`.

**Тесты:** юнит итогов/просрочки; e2e создание→оплата→заказ; проверка права `invoices.create`.

**✓ Чек-лист:**
- [ ] инвойс создаётся продавцом, приходит в чат, оплачивается→заказ
- [ ] PDF генерируется; просрочка работает; права соблюдены; тесты/покрытие ок

===STAGE-SPLIT===
# ▶ STAGE 10 — Калькуляторы
Выполни ТОЛЬКО Stage 10.

**Сделай:** `calc-kit`: абстрактный `BaseCalculator` + конкретные классы: краска, плитка, напольные покрытия, обои, бетон/стяжка, гипсокартон, штукатурка/шпаклёвка. Каждый: вход (площадь/размеры/запас%), выход (кол-во/упаковки/мешки). Результат → подбор товаров категории (фильтр каталога) → «в корзину/отложенные». Мультиязычные подписи. UI в `main-web`.

**Тесты:** юнит формул каждого калькулятора (граничные значения, запас%, округление вверх до упаковки); e2e «посчитал→подобрал товары→в корзину».

**✓ Чек-лист:**
- [ ] все 7 калькуляторов считают верно и ведут к товарам
- [ ] тесты/покрытие ок

===STAGE-SPLIT===
# ▶ STAGE 11 — Валюта и конвертация
Выполни ТОЛЬКО Stage 11. Foundations §11.

**Сделай:** `feature/currency`. База AMD; провайдер курсов (ЦБ Армении `cba` + фолбэк) за интерфейсом; коллекция `exchange_rates`, дневное обновление + кеш + фолбэк на последний курс. Переключатель валюты отображения (из `displayCurrencies`). Все расчёты заказа/комиссии — в AMD; конвертация только для отображения.

**Тесты:** юнит конвертации/кеша/фолбэка; тест «расчёты заказа остаются в AMD при любом отображении».

**✓ Чек-лист:**
- [ ] цены отображаются в выбранной валюте, расчёты в AMD
- [ ] фолбэк курсов работает; тесты/покрытие ок

===STAGE-SPLIT===
# ▶ STAGE 12 — Лояльность (конструктор + применение)
Выполни ТОЛЬКО Stage 12. Foundations §11 (включая ★-дефолт), §15.

**Сделай:** `feature/loyalty`. Модель `loyalty_programs` (`basis: total_spend|order_count`, `tiers[{level,name,threshold,discountType,discountValue}]`). Вычисление текущего уровня покупателя = макс. tier, чей `threshold ≤ метрика`. Авто-применение скидки на оформлении к сумме после товарных скидок (percent — от суммы, amount — фикс AMD, итог ≥0). **Скидку несёт платформа** (комиссия вендора считается ДО скидки лояльности — не меняй payout вендора). `loyalty_ledger` + обновление `users.loyalty.{totalSpend,totalOrders,currentTierId}` при завершении заказа. UI баланса/уровня в `main-web`. Редактор программы — в Stage 17.

**Тесты:** юнит выбора уровня по метрике; юнит расчёта скидки (percent/amount, неотрицательность, взаимодействие со скидкой товара и комиссией — payout вендора не падает); e2e «заказ→рост метрики→новый уровень→скидка на следующем заказе».

**✓ Чек-лист:**
- [ ] уровень считается по сумме ИЛИ числу заказов (по `basis`)
- [ ] скидка применяется; метрика растёт; комиссия/payout вендора не затронуты
- [ ] тесты/покрытие ок

===STAGE-SPLIT===
# ▶ STAGE 13 — AI-поиск
Выполни ТОЛЬКО Stage 13. Foundations §13.

**Сделай:** `feature/ai-search` + провайдеры в `ai-kit` (`LlmProvider`, `EmbeddingProvider`, за интерфейсами; ключи из env; mock в тестах).
- Эндпоинт `POST /ai/search`: текст задачи → LLM со **строгой JSON-схемой** `{projectType, themes:[{name, materialCategories[], keywords[]}]}` + ретраи при невалидном JSON → гибридный подбор (текстовый матч по keywords + **vector search** по `products.embedding` через Atlas Vector Search) → результат, сгруппированный по AI-темам, с пояснением темы.
- Генерация эмбеддингов товара при создании/правке (хук в catalog). Кеш частых запросов. Бюджет/троттлинг платных вызовов из конфига.
- UI «Опишите проект» (строка+аккордеоны тем) в `main-web`.

**Тесты:** юнит парсинга/валидации JSON-ответа (вкл. ретраи на моке); юнит сборки гибридного запроса; e2e «описание→темы→товары» на мок-провайдере.

**✓ Чек-лист:**
- [ ] запрос даёт результат, сгруппированный по темам
- [ ] невалидный ответ LLM обрабатывается ретраем/фолбэком; эмбеддинги генерятся
- [ ] внешний AI за моком в тестах; тесты/покрытие ок

===STAGE-SPLIT===
# ▶ STAGE 14 — Реалистичный сид
Выполни ТОЛЬКО Stage 14. Foundations §15, §19.

**Сделай:** `tools/seed` (идемпотентный). Наполни: вендоры с реалистичными арм. названиями; дерево категорий; **80–150 товаров** (осмысленные title/description hy/ru/en, цены AMD, часть со скидкой, **часть с видео-обложкой**); отзывы/рейтинги; демо-покупатели; примеры чатов с инвойсами; курсы валют; **дефолтные шаблоны** писем/SMS/страниц; **стартовую программу лояльности**; `platform_settings` (media/aiPrompts/seo.botUserAgents). Повторный запуск — без дублей.

**Тесты:** юнит идемпотентности (повторный сид не плодит дубли); проверка целостности связей (вендор↔товары↔заказы).

**✓ Чек-лист:**
- [ ] одна команда наполняет БД убедительными данными для демо
- [ ] повторный сид безопасен; связи целостны; тесты ок

===STAGE-SPLIT===
# ▶ STAGE 15 — Кабинет продавца (vendor-web)
Выполни ТОЛЬКО Stage 15. Foundations §14, §15.

**Сделай:** `feature/vendors`, `vendor-members`, `vendor-bulk`, `analytics`, `subscriptions`.
- Товары: CRUD с обложками фото/видео.
- **Массовые действия** (`vendor-bulk`): выбор множества товаров → изменение цены (на %/на сумму/установка значения), применение/снятие скидки, изменение остатка, активация/скрытие, смена категории; превью затронутых + подтверждение; большие наборы — фоновой задачей (BullMQ).
- Заказы и статусы доставки (`vendor_orders`).
- **Суб-аккаунты и права** (`vendor_members`, `PermissionsGuard`): owner назначает права `orders.view|products.manage|analytics.view|chat.handle|invoices.create`.
- Подписка: план free/pro (без функц. ограничений пока).
- **Аналитика** (право `analytics.view`, scoped по вендору): выручка/GMV по периодам, средний чек, воронка статусов, топ-товары по продажам/просмотрам, конверсия, рейтинг во времени, остатки/алерты, экспорт CSV/Excel; aggregation pipelines + кеш.
- Вендор-UI чата и создания инвойсов (из Stage 8/9) подключи здесь.

**Тесты:** юнит bulk-операций (корректность+превью); юнит агрегатов аналитики; e2e ролей суб-аккаунтов (member без права заблокирован); e2e смены статусов заказа.

**✓ Чек-лист:**
- [ ] продавец ведёт товары/заказы; массовые действия (вкл. фоновые) работают
- [ ] права суб-аккаунтов соблюдаются; аналитика корректна и scoped; экспорт работает
- [ ] тесты/покрытие ок

===STAGE-SPLIT===
# ▶ STAGE 16 — AI-ассистент продавца
Выполни ТОЛЬКО Stage 16. Foundations §13.

**Сделай:** `feature/ai-assistant`. Промпт-управляемая генерация: итоговый промпт = **базовый шаблон из `platform_settings.aiPrompts`** (description/image/video) + промпт продавца + контекст товара.
- Описание (LLM, hy/ru/en), картинка (image-провайдер), видео-превью (ffmpeg-слайдшоу из фото; опц. video-API).
- Всё через `ai_jobs` (queued→processing→done/failed) асинхронно, с прогрессом. Учитывать лимиты медиа. Результат может стать видео-обложкой товара.

**Тесты:** юнит сборки промпта (шаблон+ввод+контекст); юнит жизненного цикла `ai_jobs`; e2e на мок-провайдерах.

**✓ Чек-лист:**
- [ ] генерация запускается промптом, идёт асинхронно, результат прикрепляется
- [ ] провайдеры за моками в тестах; тесты/покрытие ок

===STAGE-SPLIT===
# ▶ STAGE 17 — Админка (admin-web)
Выполни ТОЛЬКО Stage 17. Foundations §10–§15.

**Сделай:** `feature/disputes` + админ-разделы.
- Модерация вендоров/товаров (approve/reject, `pending`→`approved`).
- Управление комиссией (через настройку) и планами подписок.
- Споры (`disputes`): открытие/рассмотрение/решение.
- Общая аналитика: GMV, выручка платформы (= комиссии), число заказов, активность.
- **Редактор шаблонов** писем/SMS/страниц (CRUD из Stage 3, превью с подстановкой).
- **Конструктор лояльности**: `basis` + уровни (порог+тип скидки+значение), активация.
- **platform_settings**: ограничения видео/изображений, промпт-шаблоны AI, список бот-UA для SEO.
- Аудит-лог (`audit_logs`) ключевых действий.

**Тесты:** юнит сохранения/валидации настроек; e2e редактирования шаблона→рендер новых текстов; e2e конструктора лояльности→применение на покупателе; гейт admin-роли.

**✓ Чек-лист:**
- [ ] админ правит шаблоны, лояльность, медиа-лимиты, AI-промпты, бот-UA — без деплоя
- [ ] модерация/споры/аналитика работают; аудит пишется
- [ ] тесты/покрытие ок

===STAGE-SPLIT===
# ▶ STAGE 18 — CI/CD (полный) и стейджинг
Выполни ТОЛЬКО Stage 18. Foundations §17.

**Сделай:** GitHub Actions: `install → biome check → eslint → typecheck → test (порог ≥80%) → nx affected build → docker build/push (GHCR) → deploy staging`. Раздельные образы/пайплайны для `server` и 3 web. Секреты через CI/секрет-менеджер. Health-checks. Sentry DSN из env. `nx affected` для сборки/тестов только изменённого.

**Тесты:** прогон пайплайна на тестовой ветке; проверка, что красные тесты/непокрытие блокируют мерж.

**✓ Чек-лист:**
- [ ] пуш→авто-проверки+сборка+деплой стейджинга
- [ ] падение тестов/покрытия блокирует мерж; образы публикуются; стейджинг отвечает

===STAGE-SPLIT===
# ▶ STAGE 19 — Мобильное приложение (NativeScript)
Выполни ТОЛЬКО Stage 19. Foundations §1, §13.

**Сделай:** `apps/mobile` (NativeScript+Angular). Переиспользуй пакеты (`api-kit`/`state-kit`/`auth-kit`/`i18n-kit`/`design-tokens`); нативный UI в `mobile-ui-kit`. Экраны: auth+OTP, каталог (видео-обложки нативным плеером), корзина/заказ, чат, вишлист, калькуляторы, лояльность. Push (FCM/APNs), secure storage для токенов.

**Тесты:** юнит переиспользуемой логики на mobile-цели; smoke-навигация ключевых экранов.

**✓ Чек-лист:**
- [ ] приложение собирается под iOS/Android, проходит ключевые сценарии
- [ ] использует общие пакеты; push/secure storage работают; тесты ок
- [ ] (стор-публикация/подпись — ручные шаги, делаю я)

Это последний стейдж. После — пройдись по всем ✓ и дай сводный отчёт о готовности проекта.
