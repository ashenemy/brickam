# Провайдер дашбордов: подхватывает JSON из смонтированной папки.
apiVersion: 1
providers:
  - name: Brickam
    type: file
    folder: Brickam
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
